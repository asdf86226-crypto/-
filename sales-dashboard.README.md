# 판매현황 분석 대시보드

이카운트(ECOUNT) 등에서 뽑은 판매 엑셀을 올려 **주별 · 월별 · 분기별 · 연별** 기간과
**상품 · 상품그룹 · 거래처** 기준으로 판매량과 매출을 분석하는 단일 HTML 대시보드입니다.

## 사용법

1. `sales-dashboard.html` 을 브라우저로 엽니다. (`vendor/` 폴더가 같은 위치에 있어야 엑셀 읽기가 됩니다)
2. 상단 **📂 엑셀/CSV 업로드** 버튼으로 판매 파일을 올립니다.
   - 처음 열면 **샘플 데이터**가 자동으로 표시됩니다.
3. **주별 / 월별 / 분기별 / 연별** 탭으로 집계 기간을 바꿉니다.
4. **분석 기준**(상품 / 상품그룹 / 거래처)을 고르면 순위 차트·상세 표가 그 기준으로 바뀝니다.
5. **필터**로 특정 상품·상품그룹·거래처 하나만 골라 그 항목의 기간별 추이를 봅니다.

매출 금액은 **공급가액(부가세 제외)** 기준으로 계산합니다.
모든 계산은 브라우저 안에서만 이루어지며, 판매 데이터가 외부로 전송되지 않습니다.

## 엑셀 내보내기 / 표 복사

- **⬇ 엑셀 내보내기** — 현재 보고 있는 분석 기준(상품/상품분류/거래처)의 표를 저장합니다.
  - **로컬에서 HTML을 열었을 때**: `.xlsx` 로 저장 (`현재 기준 요약` + `품목별 분류` 2시트).
  - **웹 공유 링크(아티팩트)에서**: 브라우저 샌드박스가 `.xlsx` 다운로드를 막으므로 **`.csv`** 로 저장합니다(엑셀에서 바로 열림).
- **📋 표 복사** — 현재 표를 클립보드에 복사합니다. 엑셀·구글시트에 붙여넣기(Ctrl+V)하면 셀로 나뉩니다.

> **상품** 기준으로 보면 표에 **상품분류 열**이 함께 나오므로, 그 뷰를 내보내면 전체 품목의 분류표가 그대로 저장됩니다.
> 웹 링크에서 저장 시 claude.ai가 저장 확인 창을 한 번 띄웁니다(수락해야 저장됨).

## 분석 기준 (집계 뷰)

| 기준 | 설명 |
|------|------|
| **상품** | 품목명 그대로. 개별 품목 단위 분석 (품목코드 함께 표시) |
| **상품분류** | 품목명·품목코드를 키워드로 **원자재 / 수전(완제품) / 수전부속 / 악세서리** 4개 분류로 자동 집계 (그 외는 기타(미분류)) |
| **거래처** | 거래처명 단위. 거래처별 매출 순위·추이 |

### 상품분류 규칙 (키워드 기반)

`sales-dashboard.html` 의 `categoryOf()` 함수에서 아래 순서로 판정합니다.

| 분류 | 대표 키워드 |
|------|------------|
| **원자재** | 잉곳 · 광재 · 고철 · 무연황동 · 동 · 아연 · 알미늄 … |
| **악세서리** | 수건걸이 · 옷걸이 · 휴지걸이 · 청소건 · 면도경 · 선반 · 행거 · 슬라이드바 … |
| **수전부속** | 몸통 · 바디 · 하부외갑 · 격판 · 카트리지 · 편심 · 조절암 · 밸브 · 스파우트 · 토수구 · 호스 · 패킹 · 와셔 · 핸들 · 헤드 · 가공 · 도금 · 매립박스 · 에어레이터 … |
| **수전(완제품)** | 수전 · 레버식 · 세면기 · 주방(수전) · 샤워 · 꼭지 · 비데 · 대변기 · 세척밸브 … (대림 DL·K·TF-L/TF-K/TF-B 코드 포함) |

> 키워드로 못 맞춘 품목은 **기타(미분류)** 로 모입니다. 실제 태성정밀 데이터 기준 미분류는 판매량의 약 0.2%(대부분 택배·운임 등 비상품 항목)입니다.
> 분류가 실제와 다른 품목이 보이면 알려주시면 규칙을 조정하겠습니다.

## 이카운트에서 엑셀 뽑기

판매현황(또는 품목별 판매현황) 조회 화면에서 **엑셀 다운로드**로 받은 파일을 그대로 올리면 됩니다.
파일 위쪽에 제목/조회기간 같은 안내 줄이 있어도, 실제 표의 머리글 행을 자동으로 찾아 인식합니다.

### 자동으로 인식하는 컬럼 (이름이 조금 달라도 됨)

| 항목 | 인식하는 머리글 (우선순위 순) |
|------|-------------------------------|
| 날짜 | 일자 · 날짜 · 판매일 · 거래일 · 전표일자 · date |
| 수량 | 수량 · 판매수량 · 판매량 · 출고수량 · 개수 · qty |
| 매출 | 공급가액 · 매출액 · 판매금액 · 매출 · 합계금액 · 합계 · 금액 · amount (부가세·단가·원가 열은 제외, **공급가액 우선**) |
| 품목명 | 품목명 · 제품명 · 상품명 · 품명 · 품목 · 제품 · 상품 (코드 열은 제외) |
| 품목코드 | 품목코드 · 제품코드 · 상품코드 · 품번 · sku · 코드 |
| 거래처 | 거래처명 · 거래처 · 매출처 · 고객명 · 고객 · customer |

**상품** 기준으로 볼 때 상세 표 첫 열에 **품목코드**가 함께 표시됩니다(상품 필터·순위 툴팁에도 병기).
한 품목명에 코드가 여러 개면 묶어서 보여줍니다.

업로드 후 대시보드 상단에 **"인식됨 — 날짜: … · 수량: … · 매출: … · 품목: … · 거래처: …"** 로
어떤 컬럼이 어떻게 매핑됐는지 표시됩니다.

> 실제 이카운트 판매현황 export(제목행 + `일자·거래처명·품목코드·품목명·수량·단가·공급가액·부가세·합계·적요`,
> 약 3,400행 / 품목 845종 / 거래처 108곳)로 동작을 검증했습니다.

## 지원 형식

- `.xlsx`, `.xls` (엑셀), `.csv`
- CSV는 UTF-8 권장 (엑셀에서 "CSV UTF-8"로 저장)

## 구성 파일

- `sales-dashboard.html` — 대시보드 본체
- `vendor/xlsx.min.js` — 엑셀 파일을 브라우저에서 읽기 위한 라이브러리 (SheetJS)
